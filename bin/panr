#!/usr/bin/env python3

from pathlib import Path
import sys

# Support running from a source checkout before installation.
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from panr2.cli import run_cli


if __name__ == "__main__":
    run_cli()
